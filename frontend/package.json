{
  "name": "frontend-monorepo",
  "scripts": {
    "bootstrap": "yarn workspaces foreach run install",
    "build": "yarn workspaces foreach run build"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "dependencies": {
    "typescript": "^4.5.5"
  },
  "packageManager": "yarn@3.2.0"
}
