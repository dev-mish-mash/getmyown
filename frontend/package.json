{
  "name": "frontend-monorepo",
  "scripts": {
    "bootstrap": "yarn workspaces foreach run ",
    "build": "yarn workspaces foreach run build"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "dependencies": {
    "@yarnpkg/pnpify": "^3.1.1",
    "eslint": "^8.10.0",
    "prettier": "2.5.1",
    "typescript": "4.5.5"
  },
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "typescript": "4.5.5"
  }
}
